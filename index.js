/**
 * INDEX.JS - Punto de entrada para React Native
 * Este archivo registra el componente raíz de la aplicación
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Registrar App como componente raíz
registerRootComponent(App);
